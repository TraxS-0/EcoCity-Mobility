ktor:
    application:
        modules:
            - com.ecocity.ApplicationKt.module
    deployment:
        port: 8080

database:
  url: ${DB_URL}
  user: ${DB_USER}
  password: ${DB_PASSWORD}
  driver: ${DB_DRIVER}
  maxPoolSize: ${DB_POOL_SIZE}